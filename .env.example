# --- Authentication ---------------------------------------------------------
# X requires a logged-in session to view most content. Provide your session
# cookies (recommended) OR run the interactive login once to persist a session.
#
# How to get these: log in to x.com in a browser, open DevTools > Application >
# Cookies > https://x.com, and copy the values.
X_AUTH_TOKEN=
X_CSRF_TOKEN=

# --- Browser ----------------------------------------------------------------
# Run the browser headless (true) or visible (false). Use false for the first
# interactive login so you can solve any challenge.
X_HEADLESS=true

# Persistent browser profile directory (stores cookies between runs).
X_USER_DATA_DIR=.x-mcp-data

# Browser channel for Patchright stealth. "chrome" is most stealthy and needs
# a real Chrome install; "chromium" falls back to the bundled browser.
X_BROWSER_CHANNEL=chrome

# Per-navigation timeout in milliseconds.
X_NAV_TIMEOUT_MS=30000

# --- Logging ----------------------------------------------------------------
X_LOG_LEVEL=INFO
